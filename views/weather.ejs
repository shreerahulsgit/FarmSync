<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather Forecast</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-bg: #ffffff;
            --secondary-bg: #f5f5f5;
            --text-color: #333333;
            --accent-color: #000000;
            --card-bg: #ffffff;
            --card-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            --border-color: #e0e0e0;
            --highlight: #555555;
            --alert-bg: #ffe9e9;
            --alert-border: #ff8080;
            --alert-text: #d32f2f;
            
            /* Icon Colors */
            --sun-color: #FF9800;
            --cloud-color: #78909C;
            --rain-color: #039BE5;
            --snow-color: #90CAF9;
            --fog-color: #B0BEC5;
            --wind-color: #7CB342;
            --temp-high-color: #F44336;
            --temp-low-color: #2196F3;
            --wind-icon-color: #009688;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--primary-bg);
            color: var(--text-color);
            margin: 0;
            padding: 0;
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 40px 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 40px;
            padding-bottom: 20px;
            border-bottom: 1px solid var(--border-color);
        }

        .title {
            font-size: 2.2rem;
            font-weight: 600;
            margin: 0;
            color: var(--accent-color);
        }

        .refresh-btn {
            background: var(--accent-color);
            color: white;
            border: none;
            padding: 10px 20px;
            margin: 30px 0;
            font-size: 0.9rem;
            cursor: pointer;
            border-radius: 4px;
            font-weight: 500;
            transition: background 0.2s ease, transform 0.2s ease;
        }

        .refresh-btn:hover {
            background: #333333;
            transform: translateY(-2px);
        }

        .current-info-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin: 30px auto;
            max-width: 700px;
            background: var(--card-bg);
            padding: 25px 40px;
            border-radius: 8px;
            box-shadow: var(--card-shadow);
            border: 1px solid var(--border-color);
        }

        .current-temp-container {
            flex: 1;
        }

        .location-info {
            flex: 1;
            text-align: right;
        }

        .current-temp-text {
            font-size: 1rem;
            font-weight: 400;
            margin-bottom: 5px;
            color: var(--highlight);
        }

        .current-temp-value {
            font-size: 2.5rem;
            font-weight: 600;
            margin: 0;
            color: var(--accent-color);
            display: flex;
            align-items: center;
        }

        .current-temp-value i {
            margin-right: 10px;
            color: var(--temp-high-color);
        }

        .location-info p {
            font-size: 0.9rem;
            margin: 5px 0;
            color: var(--highlight);
        }

        .location-info p i {
            margin-right: 5px;
        }

        .weather-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 20px;
            margin-top: 30px;
        }

        .weather-card {
            background: var(--card-bg);
            padding: 20px;
            border-radius: 8px;
            box-shadow: var(--card-shadow);
            transition: transform 0.3s ease;
            border: 1px solid var(--border-color);
        }

        .weather-card:hover {
            transform: translateY(-5px);
        }

        .weather-card h2 {
            font-size: 1rem;
            margin-top: 0;
            margin-bottom: 15px;
            color: var(--accent-color);
            font-weight: 600;
        }

        .weather-icon {
            font-size: 2.5rem;
            margin: 15px 0;
        }

        .weather-card p {
            margin: 8px 0;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
        }

        .weather-card p i {
            margin-right: 8px;
            width: 18px;
            text-align: center;
        }

        .weather-condition {
            font-weight: 600;
            font-size: 1.1rem;
            margin: 10px 0;
        }

        .loading {
            text-align: center;
            font-size: 1rem;
            color: var(--highlight);
            margin-top: 30px;
        }

        .notification-alert {
            background-color: var(--alert-bg);
            border: 1px solid var(--alert-border);
            border-radius: 8px;
            padding: 15px 20px;
            margin: 20px auto;
            max-width: 700px;
            display: flex;
            align-items: center;
            display: none;
        }

        .notification-alert i {
            font-size: 1.5rem;
            margin-right: 15px;
            color: var(--alert-text);
        }

        .notification-alert-text {
            color: var(--alert-text);
            font-weight: 500;
            margin: 0;
        }

        .notification-permission-btn {
            background-color: var(--accent-color);
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 4px;
            cursor: pointer;
            margin-left: auto;
            font-size: 0.9rem;
        }

        .rainy-card {
            border-left: 4px solid var(--rain-color);
        }

        .fa-sun {
            color: var(--sun-color);
        }

        .fa-cloud-sun {
            color: var(--cloud-color);
        }

        .fa-cloud {
            color: var(--cloud-color);
        }

        .fa-cloud-rain {
            color: var(--rain-color);
        }

        .fa-snowflake {
            color: var(--snow-color);
        }

        .fa-smog {
            color: var(--fog-color);
        }

        .fa-wind {
            color: var(--wind-color);
        }

        .fa-temperature-high {
            color: var(--temp-high-color);
        }

        .fa-temperature-low {
            color: var(--temp-low-color);
        }

        .fa-location-dot {
            color: #E53935;
        }

        .fa-globe {
            color: #1976D2;
        }

        .fa-clock {
            color: #7B1FA2;
        }

        .logo-icon {
            color: #3949AB;
            margin-right: 10px;
        }

        @media (max-width: 992px) {
            .weather-grid {
                grid-template-columns: repeat(2, 1fr);
            }
        }

        @media (max-width: 768px) {
            .current-info-container {
                flex-direction: column;
                text-align: center;
                padding: 20px;
            }
            
            .location-info {
                text-align: center;
                margin-top: 20px;
            }
            
            .weather-grid {
                grid-template-columns: repeat(1, 1fr);
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1 class="title"><i class="fas fa-cloud-sun logo-icon"></i>FarmSync Weather Forecast</h1>
        </div>
        
        <div id="notification-alert" class="notification-alert">
            <i class="fas fa-cloud-rain"></i>
            <p class="notification-alert-text" id="alert-text">Rain is forecasted in the next 24 hours.</p>
            <button id="notification-permission-btn" class="notification-permission-btn">Enable Notifications</button>
        </div>
        
        <div class="current-info-container">
            <div class="current-temp-container">
                <p class="current-temp-text">Current Temperature</p>
                <p class="current-temp-value" id="current-temp"><i class="fas fa-temperature-high"></i>--°C</p>
            </div>
            <div class="location-info">
                <p id="lat-long"><i class="fas fa-location-dot"></i>Lat: --, Lon: --</p>
                <p id="state-country"><i class="fas fa-globe"></i>Region: --, --</p>
                <p id="timezone"><i class="fas fa-clock"></i>Timezone: --</p>
            </div>
        </div>
        
        <div id="weather-container" class="weather-grid"></div>
        <p class="loading" id="loading">Fetching weather data...</p>
        <div style="text-align: center">
            <button onclick="location.reload()" class="refresh-btn">
                <i class="fas fa-sync-alt"></i> Refresh
            </button>
        </div>
    </div>

    <script>
        let notificationPermission = false;
        
        document.getElementById('notification-permission-btn').addEventListener('click', function() {
            requestNotificationPermission();
        });
        
        function requestNotificationPermission() {
            if (!("Notification" in window)) {
                alert("This browser does not support desktop notifications");
                return;
            }
            
            Notification.requestPermission().then(function(permission) {
                if (permission === "granted") {
                    notificationPermission = true;
                    document.getElementById('notification-permission-btn').style.display = 'none';
                    checkAndNotify();
                }
            });
        }
        
        function checkAndNotify() {
        }
        
        function sendRainNotification(day, date) {
            if (notificationPermission) {
                const notification = new Notification("Rain Alert", {
                    body: `Rain is forecasted for ${day}, ${date}. Remember to prepare accordingly.`,
                    icon: "https://cdn-icons-png.flaticon.com/512/4005/4005767.png"
                });
                
                notification.onclick = function() {
                    window.focus();
                    this.close();
                };
            }
        }

        navigator.geolocation.getCurrentPosition(
            (position) => {
                const lat = position.coords.latitude;
                const lon = position.coords.longitude;

                document.getElementById("lat-long").innerHTML = `<i class="fas fa-location-dot"></i> Lat: ${lat.toFixed(2)}, Lon: ${lon.toFixed(2)}`;

                const weatherAPI = `https://api.open-meteo.com/v1/forecast?latitude=${lat}&longitude=${lon}&daily=temperature_2m_max,temperature_2m_min,windspeed_10m_max,weathercode&current=temperature_2m&past_days=3&forecast_days=5&timezone=auto`;

                fetch(weatherAPI)
                    .then(response => response.json())
                    .then(data => {
                        document.getElementById("current-temp").innerHTML = `<i class="fas fa-temperature-high"></i> ${data.current.temperature_2m}°C`;
                        document.getElementById("loading").style.display = "none";

                        document.getElementById("timezone").innerHTML = `<i class="fas fa-clock"></i> Timezone: ${data.timezone}`;

                        const weatherContainer = document.getElementById("weather-container");
                        weatherContainer.innerHTML = "";
                        
                        let rainToday = false;
                        let rainTomorrow = false;
                        let firstRainyDate = null;
                        let firstRainyDay = null;

                        data.daily.time.forEach((date, index) => {
                            const maxTemp = data.daily.temperature_2m_max[index];
                            const minTemp = data.daily.temperature_2m_min[index];
                            const windSpeed = data.daily.windspeed_10m_max[index];
                            const weatherCode = data.daily.weathercode[index];
                            
                            const dateObj = new Date(date);
                            const today = new Date();
                            const tomorrow = new Date();
                            tomorrow.setDate(today.getDate() + 1);
                            
                            const isToday = dateObj.getDate() === today.getDate() && 
                                           dateObj.getMonth() === today.getMonth() &&
                                           dateObj.getFullYear() === today.getFullYear();
                                           
                            const isTomorrow = dateObj.getDate() === tomorrow.getDate() && 
                                             dateObj.getMonth() === tomorrow.getMonth() &&
                                             dateObj.getFullYear() === tomorrow.getFullYear();

                            let weatherCondition = "";
                            let icon = "";
                            let isRainy = false;
                            
                            if ([0, 1].includes(weatherCode)) {
                                weatherCondition = "Clear";
                                icon = '<i class="fas fa-sun"></i>';
                            } else if ([2, 3].includes(weatherCode)) {
                                weatherCondition = "Partly Cloudy";
                                icon = '<i class="fas fa-cloud-sun"></i>';
                            } else if ([45, 48].includes(weatherCode)) {
                                weatherCondition = "Foggy";
                                icon = '<i class="fas fa-smog"></i>';
                            } else if ([51, 53, 55, 56, 57, 61, 63, 65, 66, 67, 80, 81, 82].includes(weatherCode)) {
                                weatherCondition = "Rainy";
                                icon = '<i class="fas fa-cloud-rain"></i>';
                                isRainy = true;
                                
                                if (isToday) {
                                    rainToday = true;
                                }
                                
                                if (isTomorrow) {
                                    rainTomorrow = true;
                                }
                                
                                if (!firstRainyDate && (isToday || isTomorrow || dateObj > today)) {
                                    firstRainyDate = dateObj.toLocaleDateString('en-US', {
                                        month: 'short',
                                        day: 'numeric'
                                    });
                                    firstRainyDay = dateObj.toLocaleDateString('en-US', {
                                        weekday: 'long'
                                    });
                                }
                            } else if ([71, 73, 75, 77, 85, 86].includes(weatherCode)) {
                                weatherCondition = "Snow";
                                icon = '<i class="fas fa-snowflake"></i>';
                            } else {
                                weatherCondition = "Windy";
                                icon = '<i class="fas fa-wind"></i>';
                            }

                            const formattedDate = dateObj.toLocaleDateString('en-US', {
                                weekday: 'short',
                                month: 'short',
                                day: 'numeric'
                            });
                            
                            let cardClass = isRainy ? 'weather-card rainy-card' : 'weather-card';

                            const weatherCard = `
                                <div class="${cardClass}">
                                    <h2>${formattedDate}</h2>
                                    <div class="weather-icon">${icon}</div>
                                    <p class="weather-condition">${weatherCondition}</p>
                                    <p><i class="fas fa-temperature-high"></i> Max: <strong>${maxTemp}°C</strong></p>
                                    <p><i class="fas fa-temperature-low"></i> Min: <strong>${minTemp}°C</strong></p>
                                    <p><i class="fas fa-wind"></i> Wind: <strong>${windSpeed} km/h</strong></p>
                                </div>
                            `;

                            weatherContainer.innerHTML += weatherCard;
                        });
                        
                        if (rainToday || rainTomorrow) {
                            const alertText = document.getElementById('alert-text');
                            const alertElement = document.getElementById('notification-alert');
                            
                            if (rainToday) {
                                alertText.innerText = 'Rain is forecasted for today. Be prepared!';
                            } else if (rainTomorrow) {
                                alertText.innerText = 'Rain is forecasted for tomorrow. Be prepared!';
                            }
                            
                            alertElement.style.display = 'flex';
                            
                            checkAndNotify = function() {
                                if (notificationPermission && firstRainyDay && firstRainyDate) {
                                    sendRainNotification(firstRainyDay, firstRainyDate);
                                }
                            };
                            
                            if (notificationPermission) {
                                checkAndNotify();
                            }
                        }

                        fetch(`https://nominatim.openstreetmap.org/reverse?format=json&lat=${lat}&lon=${lon}`)
                            .then(response => response.json())
                            .then(locationData => {
                                const state = locationData.address.state || "Unknown";
                                const country = locationData.address.country || "Unknown";
                                document.getElementById("state-country").innerHTML = `<i class="fas fa-globe"></i> Region: ${state}, ${country}`;
                            })
                            .catch(() => {
                                document.getElementById("state-country").innerHTML = `<i class="fas fa-globe"></i> Region: Unknown, Unknown`;
                            });
                    })
                    .catch(() => {
                        document.getElementById("loading").innerText = "Failed to load weather data.";
                    });
            },
            () => {
                document.getElementById("loading").innerText = "Location access denied.";
            }
        );
        
        if ("Notification" in window) {
            if (Notification.permission === "granted") {
                notificationPermission = true;
                document.getElementById('notification-permission-btn').style.display = 'none';
            }
        }
    </script>
</body>
</html>